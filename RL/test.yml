#TEST

# env_config:
#   background_config: "rmsc07"
#   mkt_close: "09:35:00"

# single_run_settings:
#   seed: 222
#   path: ./models/ray_tradingPG/flash_crash_order_size=100
#   passive: True
  #extra_name:

experiment:
  experiment_name: experiment_flash_crash_order_size=1e2_with_passive
  seed_number: 50
  path: ./models/ray_tradingPG/flash_crash_order_size=100
  passive: True
  #Make sure done ration is -inf

# config_extra:
#   framework: "torch"
#   timesteps_per_iteration: 1000
#   observation_filter: "MeanStdFilter"
#   num_workers: 0

#Flash Crash env config
# env_config:
#   background_config: "flash_crash"
#   background_config_extra_kvargs: "changes_fc"
#   timestep_duration: "1s"
  # mkt_close: "09:40:00"
#   first_interval: "00:00:01"
#   done_ratio: "-inf"
#   order_fixed_size: "1e7"
#   stabalise_mode: True

# Ordinary config
# env_config:
#   done_ratio: "-inf"
#   mkt_close: "16:00:00"
#   order_fixed_size: 100
  #timestep_duration: "10s"

  
